Tinytest.add('accounts-email-templates - should set the default values when none are present in the environment', function(test) {
	// TODO: Make these test actually load 'Accounts' from within the package and examine it
	test.equal('foo', 'foo', 'foo should be foo');
	test.notEqual('foo', 'bar', 'foo should not be bar');
	// x
});
